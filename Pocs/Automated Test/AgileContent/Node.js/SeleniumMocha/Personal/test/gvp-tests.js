const assert = require('assert');

describe('GVP-QA', function() {
  describe('BlackoutCollectionsTests', function() {
    it('Create', function() {
      assert.equal(true, true);
    });
    it('Edit', function() {
      assert.equal(true, true);
    });
    it('OpenEdit', function() {
      assert.equal(true, true);
    });
    it('OpenList', function() {
      assert.equal(true, true);
    });  
    it('Delete', function() {
      assert.equal(true, true);
    });     
    it('ReadOnly', function() {
      assert.equal(true, true);
    });    
    it('ReadWriteDeletePermissions', function() {
      assert.equal(true, true);
    }); 
    it('CheckAllBlocks', function() {
      assert.equal(true, true);
    });        
  });
  
  describe('CDNLivesTests', function() {
    it('Create', function() {
      assert.equal(true, true);
    });
    it('Edit', function() {
      assert.equal(true, true);
    });
    it('OpenEdit', function() {
      assert.equal(true, true);
    });
    it('OpenList', function() {
      assert.equal(true, true);
    });  
    it('Delete', function() {
      assert.equal(true, true);
    });     
    it('ReadOnly', function() {
      assert.equal(true, true);
    });    
    it('ReadWriteDeletePermissions', function() {
      assert.equal(true, true);
    }); 
    it('CheckAllBlocks', function() {
      assert.equal(true, true);
    });        
  });
});

